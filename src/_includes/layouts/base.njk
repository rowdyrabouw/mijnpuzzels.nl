<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- SEO -->
    <title>{{ title }} - {{ site.title }}</title>
    <link rel="canonical" href="{{ site.url | toCanonicalUrl }}" />
    <meta name="description" content="{{ description or site.description }}" />
    <!-- favicon etc. -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-32x32.png" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <link rel="mask-icon" href="/safari-pinned-tab.svg" color="#193153" />
    <link rel="preload" href="assets/fonts/BritannicBold.woff2" as="font" type="font/woff2" crossorigin>
    <meta name="msapplication-TileColor" content="#603cba" />
    <meta name="theme-color" content="#da532c" />
    <!-- styles -->
    <link rel="stylesheet" href="/assets/css/style.css" />
    <!-- javascript -->
    <script defer src="/assets/js/script.js"></script>
    <!-- analytics -->
    {%- if settings.environment == "PRD" %}
    <script id="plausible" defer data-domain="fitfm.nl" src="https://plausible.io/js/script.js"></script>
    {% endif %}
  </head>
  <body>
    <div class="wrapper">
      {%- include "../components/header.njk" -%}
      <div class="content">
        <aside>{%- include "../components/aside.njk" -%}</aside>
        <main>
        {{ content | safe }}
        </main>
      </div>
      {%- include "../components/footer.njk" -%}
    </div>
  </body>
</html>
